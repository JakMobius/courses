После передачи этих файликов через git, вероятно, жесткая ссылка между ними, скорее всего, потеряется.

Чтобы восстановить её как было на семинаре, нужно выполнить эти команды:

```
rm test2.txt
ln test.txt test2.txt
```